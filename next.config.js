/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "store.storeimages.cdn-apple.com",
      "i.dummyjson.com",
      "drive.google.com",
    ],
  },
};

module.exports = nextConfig;
